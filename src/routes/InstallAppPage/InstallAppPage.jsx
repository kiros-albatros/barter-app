import { Link } from "react-router-dom";
import Header from "../../components/Header/Header";
import Footer from "../../components/Footer/Footer";

const InstallAppPage = () => {
	return (
		<div>
			<Header />
			<div>InstallAppPage</div>
			<Footer />
		</div>
	);
};

export default InstallAppPage;
